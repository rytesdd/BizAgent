# ============================================
# AI 服务配置
# ============================================

# AI 服务提供商 (必填)
# 可选值: mock | ollama | kimi
# - mock:   返回固定测试回复（开发 UI 用）
# - ollama: 调用本地 Ollama
# - kimi:   调用 Kimi (Moonshot) API
AI_PROVIDER=mock

# ============================================
# Kimi (Moonshot) 配置
# ============================================

# Kimi API Key (当 AI_PROVIDER=kimi 时必填)
# 获取地址: https://platform.moonshot.cn/
KIMI_API_KEY=your_kimi_api_key_here

# Kimi 模型 (可选，默认 moonshot-v1-8k)
# 可选值: moonshot-v1-8k | moonshot-v1-32k | moonshot-v1-128k
KIMI_MODEL=moonshot-v1-8k

# ============================================
# Ollama 本地模型配置
# ============================================

# Ollama 服务地址 (可选，默认 http://localhost:11434/v1)
OLLAMA_BASE_URL=http://localhost:11434/v1

# Ollama 模型名称 (可选，默认 qwen2-vl:7b)
# 推荐模型:
#   - qwen2-vl:7b (多模态，支持图片理解，约 4.5GB)
#   - qwen2.5:7b (纯文本，综合能力强，约 4.7GB)
#   - qwen2.5:3b (纯文本，轻量快速，约 2GB)
OLLAMA_MODEL=qwen2-vl:7b

# ============================================
# DeepSeek 配置 (兼容旧版)
# ============================================

# DeepSeek API Key (可选，用于兼容旧代码)
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ============================================
# 服务配置
# ============================================

# 服务端口 (可选，默认 3000)
PORT=3000
